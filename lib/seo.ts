// Единая точка правды для SEO-разметки: адрес сайта, названия, цены.
// Всё, что попадает и в <meta>, и в JSON-LD, и в sitemap — берётся отсюда,
// чтобы разметка для поисковика не разъезжалась с тем, что видит человек
// (Яндекс и Google считают такое расхождение поводом не доверять разметке).

// Домен вынесен в env: на проде torgos.ru, но при переезде или запуске
// на поддомене менять код не придётся.
export const SITE_URL = (process.env.NEXT_PUBLIC_SITE_URL || "https://torgos.ru").replace(/\/+$/, "");

export const SITE_NAME = "ТоргОС";

// Описание — то, что показывается строкой в выдаче. Умещается в ~160 знаков,
// содержит запросы, по которым такой сервис ищут: «программа для магазина»,
// «учёт товаров», «касса», «себестоимость».
export const SITE_DESCRIPTION =
  "Программа для магазина и кафе: касса, учёт товаров и остатков, себестоимость и прибыль по каждой позиции. Работает в браузере, с телефона. 14 дней бесплатно.";

export const PRICE_START = 950;
export const PRICE_PRO = 1850;

export const OG_IMAGE = `${SITE_URL}/og.png`;
